$('#filter-group-form').html("<%= j render(partial: 'form', locals: { filter_group: @filter_group, url: admin_filter_groups_path }) %>");
$('#filter-group-form').find('#submitbutton').val('Save Filter Group');
$('#filterGroupForm').find('.modal-title').text('Add New Filter Group');
$('#filterGroupForm').removeClass('align-items-center');
// $('#filterGroupForm').addClass('align-items-start');
$('#filterGroupForm').modal('show');
$('a.remove_fields.filter_categories').first().hide();
$('a.remove_fields.filter_in_categories').first().hide();
FilterGroupFormValidation();
