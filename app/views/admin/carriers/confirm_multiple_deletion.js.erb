$('#delete-delivery-options').html("<%= j render(partial: 'carriers_confirmation_modal') %>");
selected_carriers = []
$('.carriers-checkbox-container input[type=checkbox]:checked').each(function() {
  selected_carriers.push($(this).val())
});

if (selected_carriers.length > 0) {
  $('#delivery-option-carrier-delete-confirmation-modal').modal('show');
}



