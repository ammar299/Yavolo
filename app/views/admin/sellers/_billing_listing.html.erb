<div class="tab-pane fade" id="Billing" role="tabpanel" aria-labelledby="Billing-tab">
  <h5 class="font-weight-bold">Billing</h5>
  <div id="billing-listing-div">
    <section class="billing">
      <div class="Yavolo-table">
        <table class="table">
          <thead>
            <tr>
              <th>invoice#</th>
              <th>total</th>
              <th>description</th>
              <th>Date Generated</th>
              <th>due date</th>
              <th>status</th>
            </tr>
          </thead>
          <tbody>
            <% @invoices.each do |invoice|%>
              <tr>
                <td><%=invoice.invoice_id if invoice.invoice_id.present?%></td>
                <td>£<%=invoice.total if invoice.total.present?%></td>
                <td><%=invoice.description if invoice.description.present?%></td>
                <td><%=invoice.date_generated if invoice.date_generated.present?%></td>
                <td><%=invoice.due_date if invoice.due_date.present?%></td>
                <td><%=invoice.status if invoice.status.present?%></td>
              </tr>
            <%end%>
          </tbody>
        </table>
      </div>
    </section>
  </div>
</div>