<div class="tab-pane fade" id="Billing" role="tabpanel" aria-labelledby="Billing-tab">
  <h5 class="font-weight-bold">Billing</h5>
  <div id="billing-listing-div">
    <section class="billing">
      <div class="Yavolo-table">
        <table class="table">
          <thead>
            <tr>
              <th>invoice#</th>
              <th>total</th>
              <th>description</th>
              <th>Date Generated</th>
              <th>due date</th>
              <th>status</th>
            </tr>
          </thead>
          <tbody>
            <% @invoices.each.each.with_index do |invoice,index|%>
              <tr>
                <td>YAV<%= 100 + index  %></td>
                <td><%= invoice_total(invoice) %></td>
                <td><%= invoice&.description  %></td>
                <td><%= invoice&.date_generated&.strftime('%d/%m/%Y') %></td>
                <td><%= invoice&.due_date&.strftime('%d/%m/%Y') %></td>
                <td><%= invoice&.status %></td>
              </tr>
            <%end%>
          </tbody>
        </table>
      </div>
    </section>
  </div>
</div>
