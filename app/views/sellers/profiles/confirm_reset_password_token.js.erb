$('.modal-title').text("Reset Password");
$("#update-seller-form").html("<%= j render(partial: 'sellers/profiles/send_reset_password_email', locals: {name: "#{@seller.first_name} #{@seller.last_name}", email: @seller.email } ) %>");
$('#edit-seller-form').modal('show');
