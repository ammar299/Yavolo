<%= search_form_for @q, url: sellers_orders_path, id: "seller_order_search" do |f| %>
  <input type="hidden" name="filter_type" id="seller-order-filter-type" value="<%= params[:filter_type] %>">
  <input type="hidden" name="csfname" id="csfn" value="<%= seller_order_current_search_field_name %>">
  <% if params[:q].present? && params[:q].keys.include?('s') %>
    <%= hidden_field_tag 'q[s]', params[:q][:s].to_json %>
  <% end %>
  <% if params[:per_page].present? %>
    <%= hidden_field_tag :per_page, params[:per_page] %>
  <% end %>
  <%= f.search_field seller_order_current_search_field_name.to_sym, { class: 'input-btn-sm seller-order-search-field', placeholder: 'Search your orders', value: "#{seller_order_search_field_query_param_val(params)}" } %>
  <% if params.dig(:q, seller_order_current_search_field_name.to_sym).present? %>
    <%= link_to sellers_orders_path, class: 'btn-sm cross_link_icon' do %>
      <i class="icon-cross" aria-hidden="true"></i>
    <% end %>
  <% else %>
    <i class="fa fa-search" aria-hidden="true"></i>
  <% end %>
<% end %>
