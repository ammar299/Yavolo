<%= link_to '#', class: 'dropdown-item sortby-seller-orders', data: { sortby: sortby } do %>
  <%= title %>
  <% if params.dig(:q, :s).present? && params[:q][:s].any?{ |s| s[sortby] } %>
    <i class="fa fa-check" aria-hidden="true"></i>
  <% end %>
<% end %>