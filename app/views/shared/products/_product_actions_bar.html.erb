<div class="row bottom-mee">
    <div class="col-lg-12">
        <div class="delivery-detail-footer-innerr d-flex justify-content-between flex-wrap mb-4">
            <div class="fooetr-left">
                <a href="#" class="btn btn-sm btn-radius px-4 btn-outline-white mb-2 mt-2 back-btn-action">BACK</a>
            </div>
            <div class="footer-left">
                <a href="javascript:void(0)" class="btn btn-sm btn-radius px-4 btn-outline-white mb-2 mt-2 preview_listing">PREVIEW LISTING</a>

                <% if form.object.new_record? %>
                <%= form.submit 'SAVE DRAFT', class: 'btn btn-sm btn-radius px-4 btn-outline-white mb-2 mt-2',data: { disable_with: false } %>
                <% end %>

                <% if form.object.new_record? %>
                  <%= form.submit 'APPROVE & PUBLISH', class: 'btn btn-sm btn-radius px-4 btn-outline-white mb-2 mt-2', data: { disable_with: false } %>
                <% else %>
                  <%= form.submit 'UPDATE', class: 'btn btn-sm btn-radius px-4 btn-outline-white mb-2 mt-2', data: { disable_with: false } %>
                <% end %>

            </div>
        </div>
    </div>
</div>
