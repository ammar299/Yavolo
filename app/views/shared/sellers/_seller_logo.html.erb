<div>
  <h5 class='text-center'><%= form_headings('Logo' , 'Your Logo').html_safe %></h5>
  <p class="mt-3 mm-text">
    <span class="mb-3">
      <% if logo.present? && logo.name.url.present? %>
        <%= image_tag logo.name.url %>
      <% else %>
        <span class="icon-manage-Yavolo seller-default-logo"></span>
      <% end %>
    </span>
  </p>
  <p class="text-center">Image must be a .jpg, .gif or .png file smaller
            than 10MB and at least 500px by 500px.</p>
</div>
